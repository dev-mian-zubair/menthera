
export enum AppTableKeys {
  USERS = 'users',
  AGENTS = 'agents',
  MESSAGES = 'messages',
  CALLS = 'calls',
  ROOMS = 'rooms',
  QUEST_SESSIONS = 'quest-sessions',
  QUEST_DEFINITIONS = 'quest-definitions',
  USER_ACTIVITY = 'user-activity',
  USER_STREAKS = 'user-streaks',
  USER_ACHIEVEMENTS = 'user-achievements',
  SUBSCRIPTION_AUDIT = 'subscription-audit',
  WEBHOOK_IDEMPOTENCY = 'webhook-idempotency',
}
